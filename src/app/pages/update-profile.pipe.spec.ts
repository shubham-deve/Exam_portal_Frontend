import { UpdateProfilePipe } from './update-profile.pipe';

describe('UpdateProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
